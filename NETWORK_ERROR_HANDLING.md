# 🔄 Улучшенная обработка сетевых ошибок

## ✅ Что исправлено

### Проблема: "Failed to fetch" показывал alert
**Было:** При любой сетевой ошибке показывался alert, даже если данные сохранены локально

**Стало:** 
- Автоматические повторные попытки с экспоненциальной задержкой
- Нет alert для сетевых ошибок (данные сохранены локально)
- Alert только для критических ошибок БД

## 🛡️ Новая система обработки ошибок

### 1. **Автоматический Retry с экспоненциальной задержкой**

```javascript
// Повторные попытки:
// Попытка 1: сразу
// Попытка 2: через 1 секунду
// Попытка 3: через 2 секунды  
// Попытка 4: через 4 секунды
// Максимум: 10 секунд задержка
```

**Что повторяется:**
- Получение данных из БД
- DELETE операции
- UPDATE операции
- INSERT операции

**Типы ошибок для retry:**
- `Failed to fetch` (сетевые ошибки)
- HTTP 502, 503, 504 (временные ошибки сервера)
- HTTP 429 (слишком много запросов)

### 2. **Умная обработка сетевых ошибок**

**Сетевые ошибки:**
- ✅ Данные НЕ восстанавливаются из резерва (они уже правильные локально)
- ✅ НЕТ alert (не беспокоим пользователя)
- ✅ Данные сохраняются в localStorage
- ✅ Синхронизация произойдет автоматически при следующем изменении

**Другие ошибки:**
- ✅ Восстанавливаем данные из резерва
- ✅ Показываем alert только для критических ошибок
- ✅ Не показываем для некритических (PGRST116, 42883)

### 3. **Логирование**

Все ошибки логируются в консоль с деталями:
- Тип ошибки
- Сообщение
- Код ошибки
- Детали и подсказки

## 📊 Поведение при разных ошибках

### Сетевая ошибка (Failed to fetch)
```
1. Retry до 3 раз с задержкой
2. Если не удалось - данные остаются локально
3. НЕТ alert
4. Синхронизация при следующем изменении
```

### Временная ошибка сервера (502, 503, 504)
```
1. Retry до 3 раз с задержкой
2. Если не удалось - данные остаются локально
3. НЕТ alert
4. Синхронизация при следующем изменении
```

### Ошибка авторизации (401)
```
1. НЕТ retry (не повторяем)
2. Восстанавливаем данные из резерва
3. Показываем alert
```

### Ошибка валидации (400)
```
1. НЕТ retry (не повторяем)
2. Восстанавливаем данные из резерва
3. Показываем alert
```

## 🔍 Примеры работы

### Пример 1: Пропало интернет-соединение
```
1. Пользователь редактирует товар
2. Данные сохраняются локально
3. Попытка синхронизации → Failed to fetch
4. Retry 3 раза → все неудачно
5. Данные остаются локально
6. НЕТ alert
7. При восстановлении сети → синхронизация автоматически
```

### Пример 2: Временная ошибка сервера
```
1. Пользователь редактирует товар
2. Попытка синхронизации → 503 Service Unavailable
3. Retry через 1 секунду → успех
4. Данные синхронизированы
5. НЕТ alert
```

### Пример 3: Критическая ошибка БД
```
1. Пользователь редактирует товар
2. Попытка синхронизации → ошибка RLS
3. НЕТ retry (не сеть)
4. Восстанавливаем данные из резерва
5. Показываем alert с деталями
```

## 🎯 Преимущества

1. ✅ **Меньше беспокойства пользователя** - нет alert для временных сетевых ошибок
2. ✅ **Автоматическое восстановление** - retry при временных проблемах
3. ✅ **Надежность** - данные не теряются при сетевых проблемах
4. ✅ **Производительность** - экспоненциальная задержка не перегружает сервер
5. ✅ **Умная обработка** - разные стратегии для разных типов ошибок

## 📝 Логи в консоли

При сетевой ошибке вы увидите:
```
⚠️ Сетевая ошибка синхронизации
📦 Данные сохранены локально. Синхронизация произойдет автоматически при восстановлении соединения.
🔄 Повторная попытка 1/3 через 1000ms...
🔄 Повторная попытка 2/3 через 2000ms...
```

При успешной синхронизации:
```
✅ Данные успешно синхронизированы с PostgreSQL
```

