# 🔧 Быстрая проверка и восстановление данных

## Проверка localStorage в консоли браузера

Откройте консоль (F12) и выполните:

```javascript
// Проверка товаров в кэше
const cachedItems = localStorage.getItem('sumki-items');
if (cachedItems) {
  const items = JSON.parse(cachedItems);
  console.log('✅ Найдено товаров в кэше:', items.length);
  console.log('Товары:', items);
} else {
  console.log('❌ Товары в кэше не найдены');
}

// Проверка категорий в кэше
const cachedCategories = localStorage.getItem('sumki-empty-categories');
if (cachedCategories) {
  const categories = JSON.parse(cachedCategories);
  console.log('✅ Найдено категорий в кэше:', categories.length);
  console.log('Категории:', categories);
} else {
  console.log('❌ Категории в кэше не найдены');
}

// Показать все ключи localStorage
console.log('Все ключи localStorage:', Object.keys(localStorage));
```

## Если данные есть в кэше, но не восстанавливаются:

1. Перезагрузите страницу (Ctrl+R или Cmd+R)
2. Если не помогло, выполните в консоли:
```javascript
// Принудительная загрузка из кэша
const cachedItems = localStorage.getItem('sumki-items');
if (cachedItems) {
  const items = JSON.parse(cachedItems);
  console.log('Восстанавливаю', items.length, 'товаров...');
  // Данные восстановятся автоматически при следующей перезагрузке
  // или создайте новый товар, чтобы запустить синхронизацию
}
```

## Проверка данных в Supabase

1. Откройте Supabase Dashboard → SQL Editor
2. Выполните:

```sql
-- Узнайте свой user_id (замените email)
SELECT id, email FROM auth.users WHERE email = 'ваш@email.com';

-- Проверьте товары в базе
SELECT COUNT(*) as total_items FROM items;

-- Проверьте товары с вашим user_id (замените UUID)
SELECT * FROM items WHERE user_id = 'ваш-uuid';

-- Проверьте товары без user_id (могут быть потеряны)
SELECT * FROM items WHERE user_id IS NULL;
```

## Если товары найдены, но с другим user_id:

```sql
-- Обновите user_id для ваших товаров (замените UUID)
UPDATE items 
SET user_id = 'ваш-uuid' 
WHERE user_id IS NULL 
   OR user_id != 'ваш-uuid';
```

## Если товары не найдены нигде:

1. Данные были потеряны
2. Создайте новые товары - они будут сохраняться
3. Проверьте, работает ли синхронизация (создайте товар и проверьте в Supabase)

