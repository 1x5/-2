# 🔍 Проверка данных в Supabase

## Как проверить данные в Supabase Dashboard

### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на https://supabase.com/dashboard
2. Войдите в свой аккаунт
3. Выберите проект `orrkwxgfoafyenudouqh`

### Шаг 2: Проверка через SQL Editor (рекомендуется)

1. Откройте **SQL Editor** в левом меню
2. Откройте файл `check_database.sql` (я создал его)
3. Скопируйте запросы по одному и выполняйте их

**Самый важный запрос:**
```sql
-- Проверка всех товаров в базе
SELECT 
  id,
  user_id,
  name,
  category,
  quantity,
  created_at
FROM items
ORDER BY created_at DESC
LIMIT 50;
```

### Шаг 3: Проверка через Table Editor

1. Откройте **Table Editor** в левом меню
2. Выберите таблицу `items`
3. Посмотрите, есть ли там строки

**Если таблица пуста:**
- Данные либо удалены
- Либо не были сохранены из-за ошибки RLS
- Либо были созданы с другим user_id

### Шаг 4: Проверка пользователей

1. Откройте **Authentication** → **Users**
2. Найдите свой email
3. Скопируйте `User UID` (это ваш user_id)

## Восстановление данных

### Если данные есть, но с другим user_id:

1. Откройте `recover_data.sql`
2. Выполните шаги по порядку
3. **ВАЖНО:** Сначала создайте бэкап!

### Если данные полностью потеряны:

К сожалению, если данные удалены из Supabase, их можно восстановить только если:
- У вас есть бэкап базы данных
- У вас Premium план Supabase (автоматические бэкапы)
- Данные есть в кэше браузера (localStorage)

## Проверка кэша браузера

Если данные потеряны в базе, но есть в кэше браузера:

1. Откройте приложение в браузере
2. Откройте консоль (F12)
3. Выполните:
```javascript
const cached = localStorage.getItem('sumki-items');
if (cached) {
  const items = JSON.parse(cached);
  console.log('Найдено товаров в кэше:', items.length);
  console.log('Товары:', items);
}
```

4. Если данные есть, создайте новый товар - система автоматически синхронизирует все данные из кэша с базой

## Получение бэкапов из Supabase

### Если у вас Premium план:

1. Supabase Dashboard → **Settings** → **Database**
2. Раздел **Backups**
3. Выберите нужную дату
4. Скачайте бэкап и восстановите

### Если у вас Free план:

К сожалению, автоматические бэкапы недоступны. Но можно:
- Экспортировать данные через SQL (если они еще есть)
- Использовать данные из localStorage браузера
- Создать ручной бэкап через SQL dump

## Экспорт данных (если они есть)

```sql
-- Экспорт всех товаров в JSON формат
SELECT json_agg(row_to_json(items)) 
FROM items;

-- Экспорт товаров конкретного пользователя
SELECT json_agg(row_to_json(items)) 
FROM items 
WHERE user_id = 'YOUR_USER_ID'::uuid;
```

## Быстрая проверка

Выполните этот запрос в SQL Editor:

```sql
SELECT 
  (SELECT COUNT(*) FROM items) as total_items,
  (SELECT COUNT(*) FROM items WHERE user_id IS NULL) as items_without_user,
  (SELECT COUNT(*) FROM empty_categories) as total_categories,
  (SELECT COUNT(*) FROM auth.users) as total_users;
```

Это покажет общую статистику базы данных.

