# 📋 Инструкция по созданию таблицы логов

## Важно: Создайте таблицу в Supabase!

Для работы логов действий необходимо выполнить SQL-скрипт в Supabase.

## Шаги:

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Выполните скрипт из файла `create_action_logs_table.sql`
3. Проверьте, что таблица создана:
   - Table Editor → должна появиться таблица `action_logs`

## Проверка работы:

После создания таблицы:
- При входе с любого устройства логи будут загружаться из Supabase
- Все действия будут сохраняться в Supabase и синхронизироваться между устройствами

## Если таблица не создана:

- Логи будут сохраняться только локально (в localStorage)
- На новом устройстве логи не будут видны (так как localStorage пустой)
- После создания таблицы новые логи начнут сохраняться в Supabase

## Проверка наличия таблицы:

Выполните в SQL Editor:
```sql
SELECT * FROM action_logs LIMIT 1;
```

Если таблица существует - вы увидите результат или пустой ответ.
Если таблицы нет - будет ошибка "relation does not exist".

