name: Supabase Backup

on:
  schedule:
    # Каждые 6 часов (UTC)
    - cron: '0 */6 * * *'
  workflow_dispatch: # Ручной запуск

jobs:
  backup:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write # Нужно для коммита бэкапов
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Create backups directory
        run: mkdir -p backups
      
      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
      
      - name: Backup database
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          # Формируем имя файла с датой и временем
          TIMESTAMP=$(date -u +"%Y%m%d_%H%M%S")
          BACKUP_FILE="backups/supabase_backup_${TIMESTAMP}.sql"
          
          echo "🔍 Проверка формата URL..."
          echo "URL (первые 50 символов): ${SUPABASE_DB_URL:0:50}..."
          
          # Используем прямой URL - pg_dump сам правильно парсит его
          echo "🔄 Запуск pg_dump через прямой URL..."
          
          # Пробуем подключиться через прямой URL
          pg_dump "$SUPABASE_DB_URL" \
            --no-owner \
            --no-acl \
            --clean \
            --if-exists \
            --verbose \
            -f "$BACKUP_FILE" 2>&1 | tee backup.log || DUMP_ERROR=$?
          
          # Проверяем результат
          if [ -n "$DUMP_ERROR" ] || [ ! -f "$BACKUP_FILE" ] || [ ! -s "$BACKUP_FILE" ]; then
            echo "❌ Прямой URL не сработал"
            echo "📄 Логи ошибки:"
            cat backup.log
            echo ""
            echo "💡 Диагностика проблемы:"
            echo ""
            echo "Ошибка 'Tenant or user not found' обычно означает:"
            echo "  1. Неправильный project-ref в формате пользователя"
            echo "  2. Неправильный пароль (должен быть закодирован: \$ → %24, @ → %40)"
            echo "  3. Connection Pooling не включен или настроен неправильно"
            echo ""
            echo "Проверьте в Supabase Dashboard → Settings → Database:"
            echo "  - Connection Pooling должен быть включен"
            echo "  - Используйте Session mode для pg_dump"
            echo ""
            echo "Правильный формат URL для Connection Pooling:"
            echo "  postgresql://postgres.orrkwxgfoafyenudouqh:Kraba%24et%40Supabase@aws-0-eu-central-1.pooler.supabase.com:6543/postgres"
            echo ""
            echo "Где:"
            echo "  - postgres.orrkwxgfoafyenudouqh - формат пользователя для pooling (с точкой!)"
            echo "  - Kraba%24et%40Supabase - пароль с закодированными символами"
            echo "  - aws-0-eu-central-1.pooler.supabase.com:6543 - pooling хост"
            exit 1
          fi
          
          # Сжимаем бэкап
          gzip "$BACKUP_FILE"
          
          # Проверяем размер сжатого файла
          BACKUP_SIZE=$(stat -f%z "${BACKUP_FILE}.gz" 2>/dev/null || stat -c%s "${BACKUP_FILE}.gz" 2>/dev/null || echo "0")
          echo "✅ Бэкап создан: ${BACKUP_FILE}.gz (размер: ${BACKUP_SIZE} байт)"
      
      - name: Clean old backups (keep last 30)
        run: |
          cd backups
          if [ -n "$(ls -A *.sql.gz 2>/dev/null)" ]; then
            ls -t *.sql.gz | tail -n +31 | xargs -r rm -f || true
            echo "🧹 Старые бэкапы удалены (оставлено последние 30)"
          else
            echo "ℹ️ Нет бэкапов для очистки"
          fi
      
      - name: Commit and push backups
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add backups/
          if git diff --staged --quiet; then
            echo "ℹ️ Нет изменений для коммита"
          else
            git commit -m "Auto backup: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
            git remote -v
            git push origin main
            echo "✅ Бэкап закоммичен и отправлен"
          fi

