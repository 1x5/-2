name: Supabase Backup

on:
  schedule:
    # Каждые 6 часов (UTC)
    - cron: '0 */6 * * *'
  workflow_dispatch: # Ручной запуск

jobs:
  backup:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write # Нужно для коммита бэкапов
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Create backups directory
        run: mkdir -p backups
      
      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
      
      - name: Backup database
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          # Формируем имя файла с датой и временем
          TIMESTAMP=$(date -u +"%Y%m%d_%H%M%S")
          BACKUP_FILE="backups/supabase_backup_${TIMESTAMP}.sql"
          
          echo "🔍 Проверка формата URL..."
          echo "URL (первые 50 символов): ${SUPABASE_DB_URL:0:50}..."
          
          # Пробуем использовать прямой URL-формат для pg_dump
          # Это должно работать лучше для Connection Pooling
          echo "🔄 Запуск pg_dump через URL..."
          
          # Используем прямой URL, pg_dump сам разберет его
          pg_dump "$SUPABASE_DB_URL" \
            --no-owner \
            --no-acl \
            --clean \
            --if-exists \
            --verbose \
            -f "$BACKUP_FILE" 2>&1 | tee backup.log || DUMP_ERROR=$?
          
          # Проверяем результат
          if [ -n "$DUMP_ERROR" ] || [ ! -f "$BACKUP_FILE" ] || [ ! -s "$BACKUP_FILE" ]; then
            echo "❌ Первая попытка не удалась, пробуем альтернативный способ..."
            
            # Извлекаем компоненты из URL для использования с PGPASSWORD
            DB_URL_NO_PROTOCOL="${SUPABASE_DB_URL#postgresql://}"
            DB_USER_PASS="${DB_URL_NO_PROTOCOL%%@*}"
            DB_HOST_PORT_DB="${DB_URL_NO_PROTOCOL#*@}"
            
            DB_USER="${DB_USER_PASS%%:*}"
            DB_PASS="${DB_USER_PASS#*:}"
            DB_HOST="${DB_HOST_PORT_DB%%:*}"
            DB_PORT_DB="${DB_HOST_PORT_DB#*:}"
            DB_PORT="${DB_PORT_DB%%/*}"
            DB_NAME="${DB_PORT_DB#*/}"
            
            # Декодируем URL-encoded пароль
            DB_PASS_DECODED=$(echo -n "$DB_PASS" | sed 's/%24/$/g' | sed 's/%40/@/g' | sed 's/%3A/:/g' | sed 's/%2F/\//g')
            
            echo "📋 Параметры подключения:"
            echo "  Host: $DB_HOST"
            echo "  Port: $DB_PORT"
            echo "  User: $DB_USER"
            echo "  Database: $DB_NAME"
            
            # Устанавливаем PGPASSWORD
            export PGPASSWORD="$DB_PASS_DECODED"
            
            # Пробуем подключиться с явными параметрами
            pg_dump \
              --host="$DB_HOST" \
              --port="$DB_PORT" \
              --username="$DB_USER" \
              --dbname="$DB_NAME" \
              --no-owner \
              --no-acl \
              --clean \
              --if-exists \
              --verbose \
              -f "$BACKUP_FILE" 2>&1 | tee -a backup.log || true
            
            unset PGPASSWORD
          fi
          
          # Проверяем, что файл создан и не пустой
          if [ ! -f "$BACKUP_FILE" ] || [ ! -s "$BACKUP_FILE" ]; then
            echo "❌ Ошибка: бэкап не создан или пустой"
            echo "📄 Последние строки лога:"
            tail -30 backup.log || true
            echo ""
            echo "💡 Возможные причины:"
            echo "   1. Неправильный пароль в SUPABASE_DB_URL"
            echo "   2. Неправильный project-ref"
            echo "   3. Connection Pooling не настроен или отключен"
            exit 1
          fi
          
          # Сжимаем бэкап
          gzip "$BACKUP_FILE"
          
          # Проверяем размер сжатого файла
          BACKUP_SIZE=$(stat -f%z "${BACKUP_FILE}.gz" 2>/dev/null || stat -c%s "${BACKUP_FILE}.gz" 2>/dev/null || echo "0")
          echo "✅ Бэкап создан: ${BACKUP_FILE}.gz (размер: ${BACKUP_SIZE} байт)"
      
      - name: Clean old backups (keep last 30)
        run: |
          cd backups
          if [ -n "$(ls -A *.sql.gz 2>/dev/null)" ]; then
            ls -t *.sql.gz | tail -n +31 | xargs -r rm -f || true
            echo "🧹 Старые бэкапы удалены (оставлено последние 30)"
          else
            echo "ℹ️ Нет бэкапов для очистки"
          fi
      
      - name: Commit and push backups
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add backups/
          if git diff --staged --quiet; then
            echo "ℹ️ Нет изменений для коммита"
          else
            git commit -m "Auto backup: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
            git remote -v
            git push origin main
            echo "✅ Бэкап закоммичен и отправлен"
          fi

